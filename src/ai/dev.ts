import { config } from 'dotenv';
config();

// Le flux d'analyse des sentiments a été remplacé par une route API, donc l'importation est supprimée.
// import '@/ai/flows/entry-sentiment-analysis.ts';
