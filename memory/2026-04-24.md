# 2026-04-24

- Converted the Aurum marketing homepage toward a server-first architecture and deployed it. `src/app/(marketing)/page.tsx` is now a server component using `getTranslations`; static marketing sections render on the server, the FAQ uses native `<details>`, and only `HeroIntegrated` plus the new `src/components/marketing/floating-home-cta.tsx` remain as conversion-focused client islands. Build route `/` size dropped from 14.7 kB to 4.73 kB, First Load JS from 260 kB to 250 kB. `npm run verify` passed. Firebase Hosting + App Hosting deployed, and live checks confirmed `aurumdiary.com` and `/fr` return 200 with server-rendered marketing content and sitemap intact.
- Reduced the `NextIntlClientProvider` client payload and deployed it. Middleware now forwards normalized `x-aurum-path`; `src/app/layout.tsx` selects route-specific client messages instead of serializing all i18n messages, with tighter key subsets for the homepage hero and floating CTA. Local home HTML/RSC dropped from about 131.6 KB to 116.1 KB before compression. Fresh checks passed: `npm run typecheck`, `npm run lint`, targeted homepage performance test, `npm run build`, and `npm run verify`. Runtime checks on local and live routes found no `IntlError` or missing messages. Firebase Hosting + App Hosting rollout completed, and `SEO_AURUMDIARY_2026-04-23.md` was updated with the delivery notes.
