# 2026-04-25

- Created `MEMORY.md` as a compact long-term context file for Aurum sessions, covering Daniel preferences, product positioning, technical workflow, recent work, and workspace hygiene.
- Investigated the bug where Aurum answered the first writing prompt but appeared not to react to a follow-up. Root cause found in fragile SSE parsing and final turn insertion: both `/api/reflect` and the client assumed each `data:` event arrived in one network chunk, and the conversation continuation only updated an existing Aurum turn instead of adding one if no token had created it yet. Added a shared buffered SSE parser and made the continuation insert the final Aurum turn when needed.
- Follow-up evidence showed an Aurum reflection could still appear truncated mid-sentence while being treated as complete. Added an upstream completion guard: `/api/reflect` now requires DeepSeek `[DONE]` or `finish_reason` plus non-empty text before sending `done`, otherwise it sends a stream error and skips persistence/reply-count updates. The client now removes partial Aurum turns when a stream error arrives.
- Optimized follow-up reply latency: conversation replies now skip the pattern-detection prepass and existing-pattern fetch, keeping the first deep reflection richer while making "continuer avec Aurum" start faster. Follow-up max tokens were reduced from 1000 to 520, with very short replies still capped at 220.
- Addressed beta report UX issues: email signup now persists the accepted terms state and repairs it after email verification if the Cloud Function initialized legal settings later, preventing a duplicate CGU modal. Saving a page now starts the first Aurum reflection automatically, removing the confusing extra "Reveal" click after save.
- Fixed the remaining entry-page chat timeout path from the re-test: `MagazineEntryEditor` now uses the shared buffered SSE parser and a 22s idle timeout, aborts silent streams with a visible toast, and keeps the user's optimistic message visible. `/api/reflect` now persists follow-up user messages before waiting for the model stream, so refreshes retain the question even if Aurum times out before replying.
- Fixed an over-aggressive interruption guard introduced by the timeout work: first reflections could legitimately take 30-40s, but `/api/reflect` still aborted the upstream request after 20s, producing "La réponse d'Aurum a été interrompue" after page save. First reflections now get a 70s upstream timeout and 65s client idle window, while chat follow-ups keep shorter 30s/22s limits.
- Re-investigated the beta P0 chat failure after prod still showed no Aurum reply. Cloud Run logs showed `/api/reflect` returned HTTP 200 but logged `DeepSeek reflection stream ended incomplete` with `hasText: false` and `upstreamCompleted: true`, meaning the upstream stream ended cleanly without user-facing text. Added a fallback model path: follow-up chat tries the fast model first, initial reflections fall back to the fast model if the primary stream completes empty. Also added a client ref guard against duplicate sends and stopped repopulating the chat input after a failed send.
- Investigated the Portuguese beta failure. Prod logs for entry `JM5lqtsW8tFJLumocEAL` showed the fallback path was running, but both model attempts still completed with no text. The deeper root cause was `src/lib/ai/language.ts`: it only scored French/English despite the product policy listing ES/IT/DE/PT, and strict instructions for `pt`/`it` could throw. Fixed detection/instructions for all reflection languages, added exact PT regression cases, localized short-follow-up/priority instructions, and normalized known English interruption errors to French in the current chat UI.
- Rewrote the EN/FR landing copy toward a more visceral, embodied tone while preserving the existing design and logic: hero, placeholders, example reflection, use cases, science section, privacy/trust, FAQ, trial offer copy, and SEO metadata now focus on thoughts that loop, messages reread, chest tightness, and recurring emotional patterns without medical promises.
