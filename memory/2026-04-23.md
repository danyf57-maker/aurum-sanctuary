# 2026-04-23

- Audited `aurumdiary.com` SEO/technical health from the public site. Key findings: sitemap/robots/HTTPS are healthy; `/fr/privacy` and `/fr/terms` canonicalize to English; legacy `/legal/terms` and `/legal/privacy` still return `200` and canonicalize to home; `/etudes-scientifiques` and `/fr/etudes-scientifiques` are linked but absent from sitemap; guide pages are thin; Lighthouse local mobile run showed Performance 70, SEO 100, Accessibility 89, Best Practices 92, with LCP around 7.8s and contrast issues on gold/stone text.
- Executed SEO improvement pass: added `x-default` hreflang, added scientific reference pages to sitemap, added 308 redirects for legacy legal URLs, fixed FR legal canonicals, expanded the three priority guide topics in EN/FR, removed public AI/IA wording from Aurum marketing/legal/methodology surfaces, noindexed `/ai-video-models`, delayed GTM/Google One Tap loading, strengthened primary/gold contrast, added regression tests, and verified with `npm run verify`.
- Deployed the SEO pass to Firebase Hosting and Firebase App Hosting. Important distinction: `aurum-diary-prod.web.app` is served by Firebase Hosting, while `aurumdiary.com` is served by App Hosting backend `aurum-sanctuary`; both need deployment when changes must be visible everywhere. Live checks on `aurumdiary.com` confirmed updated sitemap entries, `x-default` hreflang, legacy legal redirects, and `/fr/guides/rosebud-alternative` returning `200` with `FAQPage` and `HowTo` schema.
- Completed and deployed an additional performance/experience pass: removed `framer-motion` from the marketing homepage CTA, replaced it with CSS transitions, made the scroll listener passive, memoized hero fallback/placeholder content, and declared admin analytics routes as `force-dynamic`. Build home First Load JS dropped from 297 kB to 260 kB. `npm run verify` passed, Firebase Hosting + App Hosting deployed, and live checks on `aurumdiary.com` confirmed home `200`, sitemap intact, Rosebud guide `200`, and no `framer-motion` string on the home HTML.
