- Built an executable repo harness for Aurum: `make bootstrap`, `make verify`, `.env.example`, env guard, i18n parity check, and local AGENTS files for sensitive areas.
- Fixed broken lint infrastructure by aligning `eslint`, `eslint-config-next`, and the flat config with the current Next.js 14 stack.
- Added a client/server boundary guard to block non-public env vars and server-only imports inside `'use client'` modules.
- Wired CI and privacy/security workflows to the same verification/security rails used locally.
- Local validation passed: env guard, client boundary guard, lint, typecheck, i18n parity, Next build, functions build, and full `verify.sh`.
- Known residual warnings remain during build: missing local Upstash config and expected dynamic admin analytics route warnings.
